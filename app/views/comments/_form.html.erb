<%= semantic_form_for [@product, @comment] do |f| %>
  <%= f.inputs do %>
    <%= f.input :text, label: false, required: false, :input_html => { :rows => 3 } %>
  <% end %>
  <%= f.hidden_field :parent_id, value: params[:reply_to] %>
  <%= f.submit %>
<% end %>
